Given /^the Shape is a Rectangle of Width (\d+\.?\d*) cm and Length (\d+\.\d*) cm$/ do |width, length|
  pending # express the regexp above with the code you wish you had
end

Given /^the Shape is an Ellipse of Width (\d+\.?\d*) cm and Length (\d+\.\d*) cm$/ do |width, length|
  pending # express the regexp above with the code you wish you had
end

Given /^the Shape is a Polygon with verticies at \((\d+),(\d+)\), \((\d+),(\d+)\), \((\d+),(\d+)\), \((\d+),(\d+)\)$/ do |arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8|
  pending # express the regexp above with the code you wish you had
end

When /^area is requested$/ do
  # do nothing
end

Then /^the Area should be (\d+\.?\d*) cm2$/ do |area|
  pending # express the regexp above with the code you wish you had
end

